$kind: http-request
description: "Paso 9: consulta el historial finalizado."
url: "{{baseUrl}}/api/alquileres/bicicletas/BIC-FLUJO-001"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 200', () => pm.response.to.have.status(200));
      pm.test('Historial es lista', () => pm.expect(pm.response.json()).to.be.an('array')); 
    language: text/javascript
order: 8000
