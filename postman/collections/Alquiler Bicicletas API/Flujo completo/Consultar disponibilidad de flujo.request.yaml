$kind: http-request
description: "Paso 2: verifica que la bicicleta aparece disponible."
url: "{{baseUrl}}/api/bicicletas/disponibles"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
